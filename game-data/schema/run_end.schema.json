{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://slayidlerepeat.local/schema/run_end.schema.json",
  "title": "content/run_end/run_end.json",
  "description": "M7-08 — the loc keys the death/revive (S13) and run-results (S14) screens render, in ONE document because 02 §6 makes them one moment. What is fixed here is that the screen names why the run is over in each of its three ways, captions the banked and paid figures, the session-floor count and both DROP_RUN countdowns, offers a revive and a way to collect, carries one sentence per absence that has a reason, and carries one status line per state the read and a submission can be in. additionalProperties is false on every object so the absences are enforced rather than merely observed. 🔒 There is no member for a doubled reward or for a second revive: AD_DOUBLE_RUN_REWARDS needs CLAIM_AD_REWARD, deferred to M15-03, and 02 §6's revive limit is once per run and hard. And no member carries a MAGNITUDE — every figure is read at render time and drawn beside these captions, because a number written into a translated string cannot be tuned and would be wrong in two languages at once.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "$schema",
    "_doc",
    "name",
    "label",
    "action",
    "block",
    "status"
  ],
  "$defs": {
    "locKey": {
      "description": "loc.<domain>.<id>.<field>, lowercase, dot-separated, snake_case segments — the same shape schema/loc.schema.json governs the locale files by.",
      "type": "string",
      "pattern": "^loc(\\.[a-z0-9_]+)+$"
    }
  },
  "properties": {
    "$schema": {
      "type": "string"
    },
    "_doc": {
      "type": "string",
      "minLength": 1
    },
    "name": {
      "description": "Why the run is over, in each of the three ways it can be. Three headings rather than one, because a screen that could not tell a death from a player choosing to stop would tell someone who quit that they had died.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "defeated",
        "victory",
        "abandoned"
      ],
      "properties": {
        "defeated": {
          "$ref": "#/$defs/locKey"
        },
        "victory": {
          "$ref": "#/$defs/locKey"
        },
        "abandoned": {
          "$ref": "#/$defs/locKey"
        }
      }
    },
    "label": {
      "description": "🔒 Captions only. `banked` and `payout` are drawn beside two figures rather than one, because the difference between them IS the completion multiplier and 24 §9 wants it legible. The two mercy captions are separate because 24 §9 requires each counter to name its unit rather than show a bare number, and they count different things. 🔒 `legendXp` and `soulShards` NAME THE TWO REWARD LINES, because the banked and paid captions are drawn over a pair of figures and a column of unlabelled numbers does not say which currency it is counting -- the same objection 24 §9 makes to a counter without its unit. ⚠️ `soulShards` points at tuning/currencies.json's OWN display name rather than authoring a second one: that key is already referenced and already translated, and a screen document claiming it again would be a string somebody pays to translate twice. Legend XP is not a CurrencyId and has no such name anywhere, so this document is where it is authored.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "banked",
        "payout",
        "legendXp",
        "soulShards",
        "floorItems",
        "eliteMercy",
        "bossMercy"
      ],
      "properties": {
        "banked": {
          "$ref": "#/$defs/locKey"
        },
        "payout": {
          "$ref": "#/$defs/locKey"
        },
        "legendXp": {
          "$ref": "#/$defs/locKey"
        },
        "soulShards": {
          "$ref": "#/$defs/locKey"
        },
        "floorItems": {
          "$ref": "#/$defs/locKey"
        },
        "eliteMercy": {
          "$ref": "#/$defs/locKey"
        },
        "bossMercy": {
          "$ref": "#/$defs/locKey"
        }
      }
    },
    "action": {
      "description": "The two controls. `revive` appears only where the rules would accept one AND the player can reach it; `finish` is always there, because a run that has ended must always be collectable.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "revive",
        "finish"
      ],
      "properties": {
        "revive": {
          "$ref": "#/$defs/locKey"
        },
        "finish": {
          "$ref": "#/$defs/locKey"
        }
      }
    },
    "block": {
      "description": "🔒 One sentence per absence that has a reason. `reviveSpent`: the run has used its one, so a player who finds no button is told why. `deathCostsRewards` names what the completion multiplier did, so the gap between the two figures above is explained rather than merely shown. The unentitled player gets no sentence: the only route that would serve them is an ad the game cannot show, and naming it would advertise a feature the game does not have.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "reviveSpent",
        "deathCostsRewards"
      ],
      "properties": {
        "reviveSpent": {
          "$ref": "#/$defs/locKey"
        },
        "deathCostsRewards": {
          "$ref": "#/$defs/locKey"
        }
      }
    },
    "status": {
      "description": "One line per state the read can be in, plus the two a submission can come back with.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "loading",
        "runMissing",
        "unavailable",
        "refused",
        "hostUnavailable"
      ],
      "properties": {
        "loading": {
          "$ref": "#/$defs/locKey"
        },
        "runMissing": {
          "$ref": "#/$defs/locKey"
        },
        "unavailable": {
          "$ref": "#/$defs/locKey"
        },
        "refused": {
          "$ref": "#/$defs/locKey"
        },
        "hostUnavailable": {
          "$ref": "#/$defs/locKey"
        }
      }
    }
  }
}
