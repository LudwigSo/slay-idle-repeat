[gd_scene load_steps=5 format=3]

[ext_resource type="Script" path="res://game/scenes/Hero.cs" id="1_hero"]
[ext_resource type="PackedScene" uid="uid://c37kehccn106d" path="res://game/art/chr_hero_rogue.glb" id="2_model"]
[ext_resource type="PackedScene" path="res://game/scenes/WpnSword.tscn" id="3_sword"]
[ext_resource type="PackedScene" path="res://game/scenes/WpnDagger.tscn" id="4_dagger"]

[node name="Hero" type="Node3D"]
script = ExtResource("1_hero")
LeftHandWeapon = ExtResource("3_sword")
RightHandWeapon = ExtResource("4_dagger")

; The model sits at identity: feet on the scene's origin, facing the way the glTF faces, at the
; scale it was authored at — the same convention every Greenwood enemy model ships with. A screen
; that wants the hero bigger, turned or dropped frames its OWN instance (Home.tscn does), so no
; other screen has to cancel a framing it never asked for.
[node name="Model" parent="." instance=ExtResource("2_model")]
