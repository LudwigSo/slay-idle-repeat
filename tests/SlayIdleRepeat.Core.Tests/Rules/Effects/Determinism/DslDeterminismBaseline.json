{
  "$comment": [
    "M2-17 — the `18` §8 determinism baseline over 10 000 seeded build permutations.",
    "THIS TABLE IS SELF-GENERATED AND HAS NO EXTERNAL PUBLISHER. M0-06's Hash64 rows and M0-07's canonical-encoding rows were validated against EXTERNALLY PUBLISHED vectors — xxHash64's, and Landon Curt Noll's test_fnv.c — because xxHash64 and FNV-1a are published algorithms. There is no published authority for 'build permutation -> hash', so these rows were produced by the very resolver they guard.",
    "WHAT IT THEREFORE PROVES: STABILITY. The `18` §8 pipeline is a pure function of its inputs, and its output cannot drift across a refactor or a compiler without a row here going red. That is exactly the accidental order-dependence `18` §11's parity line exists to catch. ACROSS A PLATFORM it proves nothing yet: .github/workflows/ci.yml's determinism job is 'if: false — gated: turns on with M5-12', so nothing runs this table on a second runtime today. The claim becomes true when that job does.",
    "WHY IT IS NOT AN INDEPENDENT TRANSCRIPTION, WHICH IS THE BAR M2-15 MET. Rules/Combat/CombatLogReferenceVectors.json faced the same absence of a publisher — nobody publishes 'event list -> LogHash' vectors for this game — and still refused to generate from the code under test: its rows came from an independent Python transcription of `14` §16.6's encoding table and `05` §7's field list. That route was available there because the subject was a BYTE ENCODING: one table, transcribable in an afternoon. THE REASON IT WAS NOT TAKEN HERE IS COST, AND NOTHING MORE PRINCIPLED THAN COST. The subject is a ten-step resolution pipeline — collection from ten sources, a 23-function condition gate, five arithmetic steps in ordinal effect-id order, three kinds of cap override and per-step rounding — so a transcription would have to re-derive the whole of M2-02..M2-07's arithmetic, and would be roughly the size of the code it checks. That is a scope judgement M2-17 made and a later task may unmake; it is NOT a rule against second implementations. `18` §8's headnote is about divergence between SHIPPED implementations, and §11's parity line presupposes two — neither M0-06 nor M2-15 kept its generator, both ran a throwaway once and committed only the output, which is exactly what a Python transcription of §8 would be. So the provenance bar here is genuinely lower than M2-15's, and this paragraph exists to say so rather than to explain it away.",
    "WHAT IT DOES NOT PROVE: that the encoding or the resolution order is CORRECT. Correctness comes from M2-02..M2-07's per-op and per-step unit tests, which assert `18` §8's arithmetic against the document. A baseline generated from the code under test proves only self-consistency, and calling these 'reference vectors' in M0-06's sense would claim a validation that does not exist.",
    "WHAT WAS CONFIRMED FIRST: before a single row below was written, CanonicalStateWriter was re-confirmed against FNV-1a's published vectors — Fnv1a64KnownAnswerTests and CanonicalStateWriterReferenceVectorTests, driven by Model/Snapshots/CanonicalStateWriterReferenceVectors.json. The hash function underneath these rows is externally validated even though the rows are not.",
    "SUCCESSOR: real two-runtime parity is M5-12, on Linux x64 and Android ARM64 — the iOS ARM64 leg is authored but gated off with iOS itself (`16` D34). There is one resolver in one assembly today and no client build until M7, so `18` §11's 'client and server resolvers agree' has nothing to compare against yet. NOTE THE OBLIGATION IS NOT YET BOOKED: M5-12's tracker row names the LogHash table and the command-sequence parity test and does NOT name this one. M2-17 may not edit the tracker; the carry-forward was reported to the conductor instead, and until that row is amended nothing makes M5-12 pick this table up.",
    "A FAILURE HERE IS A DETERMINISM BREAK, NEVER A TEST FIX. Regenerate only through the documented command in DslDeterminismBaselineWriter, and only after writing down why the hash moved. The writer stamps review.status = 'unreviewed'; this file's reader refuses that, so a regenerated table nobody reviewed stays red."
  ],
  "review": {
    "status": "reviewed",
    "reviewedOn": "2026-08-20",
    "reviewedBy": "remove-die-faces-reroll",
    "why": "Third issue of the table, and a VOCABULARY change only — a NARROWING one, which is new: every earlier issue widened. Removing the die's special faces and the reroll retired two ops (MODIFY_DIE_FACE, GRANT_REROLL), one condition function (DIE_FACE_COUNT), one stat (REROLL_CHARGES), one trigger parameter (ON_ROLL's faceKind) and one valueScale argument key (the same faceKind), so `18` §11's op count moved 44 -> 42, its condition count 23 -> 22, and EffectVocabularyEmissionSets lost a row from each of four lists. BuildPermutationGenerator lost the matching arms: the MODIFY_DIE_FACE shaping arm, the DIE_FACE_COUNT valueScale row, the faceKind on all three ConditionTerm shapes, and the DieFacesByKind fixture on the run view. EVERY CHUNK HASH BELOW MOVED, and `18` §11.1 documents precisely that: the generator anchors each axis by list[index % list.Count], so REMOVING a token from any vocabulary shifts every subsequent permutation exactly as inserting one does. A regeneration, not a determinism break. 🔒 NO HASH WAS REGENERATED TO TURN A RED TEST GREEN, and the order of work is what makes that checkable. The removal landed first across Core, the Application content set and every other suite; the whole solution built clean and both other test projects were fully green BEFORE this file was touched — the only red tests at regeneration time were this table's own rows, and they were red because the emission set had legitimately shrunk. No behaviour of the `18` §8 pipeline changed: M2-02..M2-07's arithmetic is untouched, the two retired ops were RUN_AND_BOARD ops §8 never aggregated, and the retired stat was one §8's non-combat skip already excluded from an actor's block. All twelve named rows kept their committed 'why' verbatim, and two of them — 'non-combat-stat-skipped' (7 -> 9) and 'condition-gated-an-effect-out' (9 -> 7) — now point at a different permutation, which is the same vocabulary shift showing through: each names the LOWEST-numbered build with its property, and narrowing the lists re-deals which build that is. The pair swapped places, which is the shape a re-deal takes rather than a drift. Every_named_row_still_names_the_permutation_its_property_first_holds_at is the case that re-derives both and would have gone red had either been written by hand. WHAT WAS NOT RE-REVIEWED: the $comment header's provenance argument is M2-17's and is unchanged — this table is still self-generated, still proves stability rather than correctness, and still rests underneath on FNV-1a's externally published vectors through CanonicalStateWriter, which Fnv1a64KnownAnswerTests and CanonicalStateWriterReferenceVectorTests confirmed green in the same assembly and the same run that wrote these rows."
  },
  "table": {
    "permutations": 10000,
    "chunkSize": 100,
    "baselineSeed": "0x4d32313700000000",
    "aggregate": "fnv1a:681e31ff98e3dbbc",
    "chunks": [
      "fnv1a:71d02c74871c1118",
      "fnv1a:5ee81db2a688d8c9",
      "fnv1a:f28811c3db722516",
      "fnv1a:c55e4eb7e78fd7c9",
      "fnv1a:c4eeb203d0d80bd4",
      "fnv1a:6afa72b6cc913de5",
      "fnv1a:a7038659dd770139",
      "fnv1a:a7fa74bbcc2db46b",
      "fnv1a:9813d909fc492e60",
      "fnv1a:62346c7092880a2e",
      "fnv1a:3533d98d5408efa8",
      "fnv1a:bce1dfbf992ee35d",
      "fnv1a:e296700194e9f5ec",
      "fnv1a:86d9910b4a77cbba",
      "fnv1a:587b80466f7cc9d3",
      "fnv1a:74c4a6e1a17a0703",
      "fnv1a:a3ad998527d6fdfa",
      "fnv1a:9ac91026de10c73c",
      "fnv1a:9318857163f19e32",
      "fnv1a:c221048ddf721c88",
      "fnv1a:f62f672a22a4293c",
      "fnv1a:36d32c78ada1e36a",
      "fnv1a:ab0710d6f36d62d1",
      "fnv1a:9dd6da9119f9f39e",
      "fnv1a:aeb03fafe79cdd93",
      "fnv1a:1758c96832f1e64b",
      "fnv1a:efc0395a7aea7258",
      "fnv1a:1b13c71680386c96",
      "fnv1a:53c332d2833161ef",
      "fnv1a:17f1672ebe36ff30",
      "fnv1a:0e517476429a2ff4",
      "fnv1a:a8cd286c19a3df38",
      "fnv1a:9ca415b184abe735",
      "fnv1a:6fb2e0b960258966",
      "fnv1a:17652cdfe726fa7c",
      "fnv1a:82b7537d068f535b",
      "fnv1a:99bc1c8e71df7c67",
      "fnv1a:6c11926e244bec5a",
      "fnv1a:9bcfb2f60bfdd9d7",
      "fnv1a:2e4e2563826f36c1",
      "fnv1a:caaeb693f072fa1b",
      "fnv1a:dba9a8cd06bc3245",
      "fnv1a:b03de22d352e7e15",
      "fnv1a:5f06f89961b94fbc",
      "fnv1a:e29f3547195e886e",
      "fnv1a:c8639eeddab6a3c8",
      "fnv1a:373712a8c95f8e58",
      "fnv1a:b4bd53469eb93bf6",
      "fnv1a:d5b176496ac96f6d",
      "fnv1a:32fecd0064ebe03e",
      "fnv1a:2c86d853f4661c5a",
      "fnv1a:237331a5fc50ec15",
      "fnv1a:7c04c5d3d0e8d2c6",
      "fnv1a:c16f29c48845ba78",
      "fnv1a:0a8c500082e12027",
      "fnv1a:7ba5fe80e93231b0",
      "fnv1a:7277f117f146a1f9",
      "fnv1a:3d90538dcd33f981",
      "fnv1a:ebb2837740a83395",
      "fnv1a:8d8d2e6122ea772f",
      "fnv1a:f39d0c09da3e84db",
      "fnv1a:10931dbe2356a42a",
      "fnv1a:45010b1e174439fd",
      "fnv1a:3be05ae91fc4802d",
      "fnv1a:8ea50b8eed4ec271",
      "fnv1a:9985cad7277d7297",
      "fnv1a:0e6faa337dfba993",
      "fnv1a:a9816ccfbb2b4b00",
      "fnv1a:9aeb876dd5275422",
      "fnv1a:3c4c6b10e5992615",
      "fnv1a:e25ad87cc02106ca",
      "fnv1a:c2fb2d3cac6ae1a4",
      "fnv1a:adfbebc154424aa8",
      "fnv1a:20512760f13fc5ba",
      "fnv1a:6aef9be0f484afb0",
      "fnv1a:118a490e70ed5761",
      "fnv1a:b748701404703c98",
      "fnv1a:c5e863532d171324",
      "fnv1a:bcdf6e92b91a6acd",
      "fnv1a:f4ea752e5d7f443b",
      "fnv1a:a48f436dcdfdc41e",
      "fnv1a:8c8aaf4a82297aa4",
      "fnv1a:22e5d949a1fdb3b7",
      "fnv1a:9e506018eadfff92",
      "fnv1a:700fa0e98be72b9b",
      "fnv1a:cfdd9fa90dee3088",
      "fnv1a:db14b9347ae1fdb4",
      "fnv1a:81115ff46f030f17",
      "fnv1a:b30dbc6e7cc8e01e",
      "fnv1a:d76e0ae2596d7afd",
      "fnv1a:275aad6de5318e06",
      "fnv1a:060c19cba451a0c1",
      "fnv1a:33b7e4d4e2ad729e",
      "fnv1a:283c6b10c7189c17",
      "fnv1a:14e7ad649355b1cf",
      "fnv1a:1c225c499b71fd47",
      "fnv1a:2174b3b721fe2381",
      "fnv1a:1a560c0c683637b6",
      "fnv1a:81139642cf5fef7d",
      "fnv1a:f4fa25f57ec566fb"
    ],
    "named": [
      { "id": "corpus-first", "permutation": 0, "hash": "fnv1a:f326e108fb0f2450", "why": "permutation 0 — the corpus's first row, and the first thing a change to the seed derivation moves" },
      { "id": "corpus-last", "permutation": 9999, "hash": "fnv1a:92ade882055a2db0", "why": "permutation 9 999 — the corpus's last row, so a corpus truncated below 10 000 fails here instead of silently covering less" },
      { "id": "above-introsort-threshold", "permutation": 1, "hash": "fnv1a:881b02043b038b5c", "why": "the lowest-numbered build not already named above that holds more than 16 effects, where .NET's introsort stops being stable and 18 §8's total order has to carry the answer on its own" },
      { "id": "at-or-below-introsort-threshold", "permutation": 16, "hash": "fnv1a:42cd46797cef820a", "why": "the lowest-numbered build not already named above that holds 16 effects or fewer — the regime a stable sort would hide a broken tiebreak in, kept in the table so the contrast exists" },
      { "id": "duplicate-ids-above-threshold", "permutation": 2, "hash": "fnv1a:b8305b50a0b12ae8", "why": "the lowest-numbered build not already named above that both exceeds 16 effects and resolves BOTH halves of the deliberate EFF_DUP pair — two effects sharing one id, separated only by EffectResolutionOrder tiebreak, in a build past the point where introsort stops being stable. M2-02 blind spot, exactly" },
      { "id": "pvp-context", "permutation": 5, "hash": "fnv1a:887b2c391cf20f0b", "why": "the lowest-numbered build not already named above that is resolved with IS_PVP true, where 18 §4's ENEMY_COUNT short-circuits to 1 and TARGET_IS_ELITE / TARGET_IS_BOSS change arm" },
      { "id": "enrage-context", "permutation": 3, "hash": "fnv1a:a0295b1015eeb12f", "why": "the lowest-numbered build not already named above whose context carries 05 §3.1's 70 s enrage, which BATTLE_TIME_REMAINING_EST reads in place of the fight horizon" },
      { "id": "stat-convert-present", "permutation": 4, "hash": "fnv1a:370aa7626a6a78ab", "why": "the lowest-numbered build not already named above carrying 18 §8 step 6's STAT_CONVERT — the step that reads the frozen post-step-5 block and 18 §10.1 E1's toStat" },
      { "id": "redirect-excess-present", "permutation": 8, "hash": "fnv1a:be8a7ff03f551321", "why": "the lowest-numbered build not already named above carrying 18 §10.1 E2's REDIRECT_EXCESS half of STAT_CAP_OVERRIDE — step 9's other half, and the only redirect any document authors" },
      { "id": "value-scale-status-argument", "permutation": 6, "hash": "fnv1a:a1f0fb2ae93eaee1", "why": "the lowest-numbered build not already named above whose valueScale carries M2-06's statusId argument, so ConditionEvaluator.Read is driven with an argument and not only a function" },
      { "id": "non-combat-stat-skipped", "permutation": 9, "hash": "fnv1a:13c1b0704d1671d3", "why": "the lowest-numbered build not already named above whose stat op names one of 18 §2.1's 12 non-combat stats, so AggregatedStats.SkippedNonCombatStatEffects is non-empty and reaches the hash" },
      { "id": "condition-gated-an-effect-out", "permutation": 7, "hash": "fnv1a:b7aa896aa7709d7f", "why": "the lowest-numbered build not already named above where 18 §8 step 2 removed an effect, so GatedOut is non-empty — a gate that admitted everything would hash just as stably" }
    ]
  }
}
