{
  "$comment": [
    "M5-12 — `14` §13's client/server parity corpus: 1 000 command sequences driven through the in-process host and through the wire gateway, their state hashes compared step by step, and the whole comparison pinned by 10 chunk hashes plus one aggregate.",
    "CHANGING A ROW IN THIS FILE IS A DETERMINISM BREAK, NEVER A TEST FIX. The only legitimate edits are ADDING rows and a reviewed regeneration through the documented command in ParityBaselineWriter. The writer stamps review.status = 'unreviewed'; this file's reader refuses that, so a regenerated table nobody reviewed stays red.",
    "WHAT IT COMPARES, AND WHY THAT IS NOT VACUOUS. Both sides reference the same SlayIdleRepeat.Core.dll, so a 'parity' test that ran one host twice would be green by construction. What is compared here is two DIFFERENT dispatch pipelines over that one rules library: the in-process host builds its GameContext and calls the use case directly, while the gateway decodes a JSON envelope, resolves a sequencing scope, consults an idempotency ledger, a throttle and a content pin, builds its own GameContext, and splits decide / commit / publish across a unit of work. Those are the two paths a command actually reaches the domain by.",
    "⚠️ WHAT IT DOES NOT COMPARE: two runtimes. One process on one architecture cannot show that a phone and a server agree about floating point. That is ci.yml's `determinism` job, on Linux x64 and Android ARM64 — the iOS ARM64 leg is authored and gated off with iOS itself (`16` D34). This corpus rides those legs too, and says nothing about them on its own.",
    "⚠️ START_RUN IS EXCLUDED, DELIBERATELY AND VISIBLY. It is the one command the two hosts are DESIGNED to disagree on: the gateway allocates the run id from its id generator, the in-process host passes no AllocatedRunId at all, and the run id is inside the hashed projection. The server is the authority on run identity, so this is correct behaviour rather than a defect — and it is pinned by its own case in ClientServerParityTests rather than left as an unexplained gap in the alphabet. Every other one of the 55 registered commands is driven.",
    "THE SEQUENCES ARE NOT COMMITTED, THE SEED IS. Each is generated by a legality-aware walker over GameRules from baselineSeed and its own ordinal: most of the registry is refused from most states, so a uniform walk would be a thousand sequences of refusals. A moved chunk localises a break to a hundred sequences, which is a diff a person can read.",
    "⚠️ THIS TABLE IS SCHEDULED TO MOVE, ONCE. D45 (a hero's HP persists across a run — owner M7-06g) and D46 (DMG% and DR% become multiplier stats — owner M4-16d) both change state these sequences reach. Re-baseline AFTER BOTH have landed, never between them, so these hashes move once rather than twice. A SchemaVersion bump moves them too, since the hashes are taken over the wire projection."
  ],
  "review": {
    "status": "reviewed",
    "reviewedOn": "2026-08-29",
    "reviewedBy": "M5-12",
    "why": "First authoring, not a move: M5-12 created this table, so every hash below is new rather than changed and there is no earlier value to diff against. What was checked before it was stamped: the two dispatch pipelines agreed on every state hash of every step of all 1 000 sequences with zero mismatches; the corpus drove all 54 registered commands the alphabet carries; more than a third of its steps were accepted rather than refused, and refusals are present too; the two id generators finished every sequence in lockstep; no sequence earned a transport-tier refusal; and the whole pass costs about 9 s, inside the unit tier. The next person to see this block unreviewed is looking at a break in one of the two pipelines or in the walker: read the chunk diff, name what moved, and only then re-stamp it."
  },
  "table": {
    "sequences": 1000,
    "chunkSize": 100,
    "baselineSeed": "0x4d35313250415259",
    "baselineState": "fnv1a:d6e2e8634485445d",
    "aggregate": "fnv1a:de66461358f26285",
    "chunks": [
      "fnv1a:dae5954af45148ef",
      "fnv1a:4415fc127f75e425",
      "fnv1a:a7c904ad356e8fec",
      "fnv1a:247659b08d2e7d90",
      "fnv1a:6321359708127dae",
      "fnv1a:5d151a09b03976fe",
      "fnv1a:9cba9e01821fab4c",
      "fnv1a:4705ec9c9abec8a5",
      "fnv1a:1bf7046b04c0b111",
      "fnv1a:9c96f7bf75b06b2a"
    ],
    "named": [
      { "id": "corpus-first", "sequence": 0, "steps": 7, "hash": "fnv1a:29d05ab8d7f91507", "why": "sequence 0 — the corpus's first row, and the first thing a change to the walker or to the seed derivation moves" },
      { "id": "corpus-last", "sequence": 999, "steps": 6, "hash": "fnv1a:7bde8a1c76bd798f", "why": "sequence 999 — the last row, so a corpus silently truncated to fewer than 14 §13's one thousand cannot resolve this one" },
      { "id": "longest-sequence", "sequence": 7, "steps": 8, "hash": "fnv1a:d3abd46ee4cbc735", "why": "the first sequence at the walker's maximum length — the deepest chain of dependent state either pipeline is asked to carry" },
      { "id": "shortest-sequence", "sequence": 26, "steps": 3, "hash": "fnv1a:590e0f35e61122d2", "why": "the first sequence at the walker's minimum length, so both ends of the length band are pinned rather than only the middle" },
      { "id": "every-step-accepted", "sequence": 45, "steps": 4, "hash": "fnv1a:fb2ee282bbdb2e32", "why": "the first sequence the domain accepted end to end — a whole walk of real state changes, with no refusal to mask a divergence by leaving the state untouched" },
      { "id": "every-step-refused", "sequence": 44, "steps": 4, "hash": "fnv1a:ccebbb35a4fde157", "why": "the first sequence the domain refused end to end — the shape in which both pipelines must agree about state that did not move, which is a different claim from agreeing about state that did" },
      { "id": "leaves-the-run", "sequence": 1, "steps": 5, "hash": "fnv1a:83fc93f6c9d5042a", "why": "the first sequence that ends or abandons its run mid-walk, so the run-already-ended gate and the hashing of a run that has stopped progressing are both pinned by a row" },
      { "id": "accepts-on-both-endpoints", "sequence": 5, "steps": 7, "hash": "fnv1a:847a63fb211f5b0e", "why": "the first sequence with an accepted command on each endpoint — the wire's run/player split is the thing the in-process host has no equivalent of, and a corpus whose acceptances all landed on one side would compare half of it" }
    ]
  }
}
