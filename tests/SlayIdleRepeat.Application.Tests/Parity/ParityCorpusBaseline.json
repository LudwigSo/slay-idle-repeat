{
  "$comment": [
    "M5-12 — `14` §13's client/server parity corpus: 1 000 command sequences driven through the in-process host and through the wire gateway, their state hashes compared step by step, and the whole comparison pinned by 10 chunk hashes plus one aggregate.",
    "CHANGING A ROW IN THIS FILE IS A DETERMINISM BREAK, NEVER A TEST FIX. The only legitimate edits are ADDING rows and a reviewed regeneration through the documented command in ParityBaselineWriter. The writer stamps review.status = 'unreviewed'; this file's reader refuses that, so a regenerated table nobody reviewed stays red.",
    "WHAT IT COMPARES, AND WHY THAT IS NOT VACUOUS. Both sides reference the same SlayIdleRepeat.Core.dll, so a 'parity' test that ran one host twice would be green by construction. What is compared here is two DIFFERENT dispatch pipelines over that one rules library: the in-process host builds its GameContext and calls the use case directly, while the gateway decodes a JSON envelope, resolves a sequencing scope, consults an idempotency ledger, a throttle and a content pin, builds its own GameContext, and splits decide / commit / publish across a unit of work. Those are the two paths a command actually reaches the domain by.",
    "⚠️ WHAT IT DOES NOT COMPARE: two runtimes. One process on one architecture cannot show that a phone and a server agree about floating point. That is ci.yml's `determinism` job, on Linux x64 and Android ARM64 — the iOS ARM64 leg is authored and gated off with iOS itself (`16` D34). This corpus rides those legs too, and says nothing about them on its own.",
    "⚠️ START_RUN IS EXCLUDED, DELIBERATELY AND VISIBLY. It is the one command the two hosts are DESIGNED to disagree on: the gateway allocates the run id from its id generator, the in-process host passes no AllocatedRunId at all, and the run id is inside the hashed projection. The server is the authority on run identity, so this is correct behaviour rather than a defect — and it is pinned by its own case in ClientServerParityTests rather than left as an unexplained gap in the alphabet. Every other one of the 55 registered commands is driven.",
    "THE SEQUENCES ARE NOT COMMITTED, THE SEED IS. Each is generated by a legality-aware walker over GameRules from baselineSeed and its own ordinal: most of the registry is refused from most states, so a uniform walk would be a thousand sequences of refusals. A moved chunk localises a break to a hundred sequences, which is a diff a person can read.",
    "⚠️ THIS TABLE WAS SCHEDULED TO MOVE FOR D45 AND D46, AND DID NOT. Both landed together on 2026-08-31 (D45 — a hero's HP persists across a run, owner M7-06g; D46 — DMG% and DR% become multiplier stats, owner M4-16d). Recomputing this corpus against them left every chunk hash and the baseline state unchanged: the sequences walked here never resolve combat, because START_RUN — the command that opens the run — is the one this corpus excludes. A SchemaVersion bump still moves these hashes, since they are taken over the wire projection."
  ],
  "review": {
    "status": "reviewed",
    "reviewedOn": "2026-09-07",
    "reviewedBy": "home-run-hub/Phase 3a",
    "why": "A RE-BASELINE, NOT A TEST FIX, and the cause is named rather than guessed at: this branch's Phase 3a made a run COST ENERGY. Handlers.StartRun now charges EnergyTuning.RunCost (20) through EnergyMath.Spend and emits the CurrencyChanged that attributes it, so the player row every sequence walks from holds a different Energy balance than it did before — and the row is inside the hashed wire projection. The corpus's own fixture had to move with it: a starting profile opens both Energy banks at zero, so GatewayWorld.WithAStartingPlayerAsync now stores a row holding exactly one run's price, or the START_RUN that opens the run every sequence acts inside would be refused and the whole corpus would walk a run-less slice. What was read before this was stamped: the WALK itself did not move. All eight named rows kept their sequence index AND their step count to the number (0/7, 999/6, 7/8, 26/3, 45/4, 44/4, 1/5, 2/7) and moved only their hash, which is the signature of a corpus whose commands and answers are unchanged and whose hashed STATE moved underneath them — as against M5-08's CLAIM_INBOX re-baseline, where a named row changed sequence outright. baselineState moved (fnv1a:d6e2e8634485445d -> fnv1a:1ae212469b1bb37f) for exactly that reason: it is the state after the fixture's START_RUN, which is now 20 Energy poorer. All ten chunk hashes and the aggregate moved, and none of them is a surprise. The two pipelines still agree on every state hash of every step with ZERO mismatches, so nothing here is a divergence being baselined away, and the corpus's own floors still hold: over 2 999 steps, refusals present, and the accepted share above 0.30. START_RUN remains the one excluded command; the change is to what it COSTS, not to which pipeline drives it. The LogHash corpus was deliberately NOT regenerated: it simulates combat, no Energy spend reaches it, and it was green on this tree untouched."
  },
  "table": {
    "sequences": 1000,
    "chunkSize": 100,
    "baselineSeed": "0x4d35313250415259",
    "baselineState": "fnv1a:1ae212469b1bb37f",
    "aggregate": "fnv1a:45824930e8f4b6be",
    "chunks": [
      "fnv1a:47be713bbeb4fda9",
      "fnv1a:bc0bab2b1506bca2",
      "fnv1a:174af12288eb32c1",
      "fnv1a:3727536826954c1c",
      "fnv1a:e9e1500b426fb1cf",
      "fnv1a:99124a01749e500b",
      "fnv1a:6166ba31aaa5ed3f",
      "fnv1a:0df9927e5f3d78d3",
      "fnv1a:485003e7a241da20",
      "fnv1a:3543a4c74777556e"
    ],
    "named": [
      { "id": "corpus-first", "sequence": 0, "steps": 7, "hash": "fnv1a:3a5b06913663d462", "why": "sequence 0 — the corpus's first row, and the first thing a change to the walker or to the seed derivation moves" },
      { "id": "corpus-last", "sequence": 999, "steps": 6, "hash": "fnv1a:65d7867086301ae4", "why": "sequence 999 — the last row, so a corpus silently truncated to fewer than 14 §13's one thousand cannot resolve this one" },
      { "id": "longest-sequence", "sequence": 7, "steps": 8, "hash": "fnv1a:1c16f9d4685eca43", "why": "the first sequence at the walker's maximum length — the deepest chain of dependent state either pipeline is asked to carry" },
      { "id": "shortest-sequence", "sequence": 26, "steps": 3, "hash": "fnv1a:77f62a44f16f1624", "why": "the first sequence at the walker's minimum length, so both ends of the length band are pinned rather than only the middle" },
      { "id": "every-step-accepted", "sequence": 45, "steps": 4, "hash": "fnv1a:173a42ce3bdcd47e", "why": "the first sequence the domain accepted end to end — a whole walk of real state changes, with no refusal to mask a divergence by leaving the state untouched" },
      { "id": "every-step-refused", "sequence": 44, "steps": 4, "hash": "fnv1a:d60c3a702019bdaa", "why": "the first sequence the domain refused end to end — the shape in which both pipelines must agree about state that did not move, which is a different claim from agreeing about state that did" },
      { "id": "leaves-the-run", "sequence": 1, "steps": 5, "hash": "fnv1a:d489c9c6e702c2ae", "why": "the first sequence that ends or abandons its run mid-walk, so the run-already-ended gate and the hashing of a run that has stopped progressing are both pinned by a row" },
      { "id": "accepts-on-both-endpoints", "sequence": 2, "steps": 7, "hash": "fnv1a:912f368096403ab7", "why": "the first sequence with an accepted command on each endpoint — the wire's run/player split is the thing the in-process host has no equivalent of, and a corpus whose acceptances all landed on one side would compare half of it" }
    ]
  }
}
